<div class="content">
  
  
  <h3>ブログ内容</h3>
  
  <!--each文の％には＝をつけないこと　下に変な文章がでてしまうよ-->
  <% @blogs.each do |blog| %>
    <div class="content-box">
      
      <P><%= blog.text %></P>
      
      <% if user_signed_in? && current_user.id == blog.user_id %>
      <%= link_to '削除', "/blogs/#{blog.id}", method: :delete %>
      <% end %>
    </div> 
  <% end %>

</div>